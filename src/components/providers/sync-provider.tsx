'use client';

/**
 * SyncProvider — placeholder for future Supabase sync.
 * Currently a pass-through wrapper.
 */
export function SyncProvider({ children }: Readonly<{ children: React.ReactNode }>) {
  return <>{children}</>;
}
